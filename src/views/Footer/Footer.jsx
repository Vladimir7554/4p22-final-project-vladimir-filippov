import React from 'react'
import './Footer.css'

const Footer = (props) => {
  const {
    className = '',
  } = props

  return (

    <header className={`${className} footer`}>
      FOOTER
    </header>

  )
  }

  export default Footer